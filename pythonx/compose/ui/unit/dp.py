"""`pythonx/compose/ui/unit/dp.py` -- `Dp` value class proxy placeholder.

## How `Dp` is represented

`Dp` parameters accept raw numeric values (e.g. `16` or `16.0`) directly on extension calls
like `padding(16)` without needing explicit `dp` wrapper objects.
"""
